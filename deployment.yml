apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pod-server
  template:
    metadata:
      labels:
        app: pod-server
    spec:
      containers:
      - name: pod-server
        image: trevorsullivan/pwsh-pode
        resources:
          limits:
            memory: "200Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
